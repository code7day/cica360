# CICA360 (front) — Estado actual

> Fuente de verdad del **dónde estamos**. Actualizar al inicio y al final de cada sesión de trabajo significativa.
> Última actualización: 2026-09-18 (**Fix real: build de CI rompía por colisión `FormData` propio vs `FormData` global del navegador — nada se desplegaba desde Fase 2 de formularios** — el Tech Lead reportó, con captura del workflow en rojo: "hay algo raro en cica360 no despliega automaticamente el trigger creo que algo se movió". El trigger SÍ disparaba, pero `astro check` fallaba en `ContactFormBlock.astro:112` (`Type 'FormData' is missing... uuid, slug, name, fields, thank_you`) y abortaba el build antes del deploy FTP. Causa: `src/lib/api.ts` declara `getForm(): Promise<FormData>` sin importar `FormData` de `./types` — TS lo resolvía al tipo ambiental global (`lib.dom.d.ts`, la clase del navegador para `multipart/form-data`), no a la interfaz propia del formulario del API. Bug preexistente desde que se agregó `getForm()` a `api.ts`, recién visible al activarse como caller real en Fase 2 (entrada de abajo, mismo día). Fix: agregado `FormData` al import de tipos en `api.ts` (shadowea el ambiental dentro del módulo, patrón estándar de TS). Verificado con `tsc --noEmit` (exit 0, mismo error que reportaba CI). Ver PROGRESS.md. **Pendiente: commit + push para que el próximo build corra en verde.** Owner: libre.)
> Antes, mismo día (**Formularios Fase 2: `ContactForm.tsx` dinámico por `FormFieldTypeEnum` (ADR-007)** — reporte en vivo del Tech Lead: "no se ha integrado el formulario, acabo de desactivar 2 campos en el admin" y el público seguía mostrando los 7 campos de siempre. `ContactFormBlock.astro` deja de ignorar `content.form_slug` — lo resuelve en build time con `getForm()` (ya existía en `api.ts`, sin caller hasta ahora) y lo pasa como prop `form: FormData`. `ContactForm.tsx` se reescribió por completo: de cero props + 7 campos hardcodeados a render dinámico de `form.fields` por `FormFieldTypeEnum` (switch sobre los 11 tipos), con validación de formato interpretando `FormFieldData.validation_rules` (`min:`/`max:`/`regex:`) en vez de reglas por nombre de campo — un campo desactivado en Studio ya no llega en el array (`FormController::show()`, genesis, ya filtraba `is_active=true` desde antes). La UX de WhatsApp (`tel_country`: bandera+código país, dígitos agrupados, preselección por IP) se preserva, ahora autocontenida por campo. Prerrequisito del lado genesis: `content.form_id` → `content.form_slug` (el API público solo resuelve por slug). Verificado con `npx tsc --noEmit -p tsconfig.json --ignoreDeprecations 6.0` (exit 0, proyecto completo) — `astro check`/`astro sync` no disponibles en este sandbox por un binding nativo de `rolldown` faltante, ajeno a este cambio. Pendiente: confirmación visual del Tech Lead en `npm run dev`/build real, y que desactivar un campo en Studio lo saque del sitio público tras el próximo rebuild. Ver ADR-007.)
> Antes, mismo día (**Proxies PHP resueltos contra `PUBLIC_SITE_URL` en dev local con MAMP + CORS mínimo (ADR-002, 2 actualizaciones)** — `npx astro dev` corre en Node y no interpreta PHP, así que las rutas relativas por defecto de `contacto.php`/`ipinfo.php` daban 404 en local contra el propio server de Astro. Nueva `resolveLocalPhpProxyEndpoint()` en `src/lib/api.ts`: en `import.meta.env.DEV`, un endpoint relativo se resuelve contra `PUBLIC_SITE_URL` (ej. `https://cica360.host`, MAMP) en vez de contra Astro; no-op en build/producción. Eso solo no alcanzó: al volverse cross-origin de verdad (`localhost:4321` → `cica360.host`), el navegador bloqueaba el fetch por falta de headers CORS (nunca hicieron falta antes, en producción comparten origen) — nuevo `public/_cors.php` compartido, `cica360_apply_local_dev_cors()` refleja `Origin` SOLO si es `localhost`/`127.0.0.1`, aplicado en `contacto.php` (+ corte de su propio `OPTIONS`) e `ipinfo.php`. Sin tocar la estrategia proxy-PHP-vs-token-acotado de ADR-002. `tsc --noEmit --ignoreDeprecations 6.0` limpio; balance de llaves/paréntesis de los 3 PHP verificado manualmente (`astro check` no corrió en este sandbox por un binding nativo de `rolldown` faltante, ajeno a este cambio). Ver ADR-002 (2 actualizaciones 2026-09-18) y PROGRESS.md.)
> Antes, mismo día (**Fix real del pipeline de deploy automático en producción (ADR-006, actualización) + gap de dots en sliders** — primera prueba end-to-end de la Fase 6 reveló 4 problemas del pipeline de GitHub Actions, corregidos uno por uno: Node 20 vs Astro `>=22.12.0` (sube a Node 22), FTP plano rechazado por el hosting (Ferozo/DonWeb exige `protocol: ftps`), `.htaccess` bloqueado por FTP con `550 Permission denied` (excluido del deploy automático, se gestiona a mano una vez), y `server-dir` apuntando a la raíz de la cuenta FTP en vez de `public_html/` (creó una copia duplicada del sitio fuera del document root, limpiada a mano). Con los 4 corregidos, corrió el primer deploy end-to-end completo en verde. Del lado genesis, 2 bugs relacionados bloqueaban que esto llegara siquiera a dispararse: un `FatalError` de composición de traits en `TriggerFrontendDeploy::$queue` (bloqueaba TODO guardado en Studio) y `DeployTriggerObserver` sin registrar en `Block` (el footer/colophon nunca disparaba el deploy) — ver `genesis/docs/context/PROGRESS.md`, ADR-072 (actualización 2026-09-18). Aparte, se corrigió el gap visual entre dots de sliders (`h-9`→`h-7` en `Hero.astro`/`Logos.astro`/`Testimonials.astro` — el "gap" era el área de toque del botón, no un `gap` CSS real).)
> Antes, 2026-09-17 (**Fase 6 (post-MVP) adelantada: deploy automático por webhook (ver ADR-006)** — el Tech Lead confirmó en producción que un cambio de contenido en Studio (título de la Home) no se reflejaba en la website — esperable en un sitio SSG puro, pero rompe la premisa de un CMS headless. Se reemplaza la decisión de deploy manual del 2026-09-13: `build-and-deploy.yml` gana el trigger `repository_dispatch` (`types: [content-updated]`) y el paso `SamKirkland/FTP-Deploy-Action` (antes comentado) pasa a correr siempre. Genesis dispara ese evento al guardar contenido de un tenant con `deploy_repo`/`deploy_token` configurados (ver ADR-072 en `genesis/docs/context/DECISIONS.md`). El artifact `cica360-dist` se mantiene como respaldo/rollback manual. **0 cambio a Astro/SSG/islands — SEO/performance intactos, solo cambia QUIÉN dispara el build.** Pendiente operativo: configurar secrets `FTP_SERVER`/`FTP_USERNAME`/`FTP_PASSWORD` en GitHub (este repo) + `deploy_repo`/`deploy_token` del tenant CICA360 en genesis. Ver ADR-006.)
> Antes, mismo día (**Animación de entrada sitewide: motor `data-reveal` (reemplaza `properties.animation`, ver ADR-005)** — pedido del Tech Lead pivotó de "usar el selector de animación existente en Filament" a "quitar ese campo desde Filament... solo de forma estática cada cliente/desarrollador implementará la animación... aplicar animación en todos los componentes de cada sección y en todas las páginas de todo el sitio, coherente, suave, sutil, amigable y elegante". Se confirmó que `properties.animation` era un campo fantasma (nunca consumido en ningún componente `.astro`) y se eliminó del lado Filament (ver `genesis/docs/context/DECISIONS.md`, ADR-070). Se construyó un motor propio: `[data-reveal="up"|"fade"]` + `IntersectionObserver` reveal-once (`src/styles/global.css`/`src/layouts/BaseLayout.astro`), con stagger vía `--reveal-delay`, respetando `prefers-reduced-motion`. Cableado en los 18 componentes de `src/components/blocks/`, en `src/pages/servicios/[slug].astro` (sin tocar header/wave/banderas, ya resuelto por otro agente) y en `src/pages/blog/*`. `Hero.astro`/`Heading.astro` quedaron sin `data-reveal` a propósito (banners "above the fold"; `Hero.astro` ya tiene su propio sistema de animación por slide). Interfaces TS locales `animation?: ...` huérfanas removidas de 5 componentes. Verificado con `@astrojs/compiler` (0 diagnósticos) y `tsc --strict` (limpio). **Constraint explícito del Tech Lead: no se hizo `git commit`/`push` ni deploy — solo para probar en local con `astro dev`.** Ver ADR-005 en este repo.)
> Antes, 2026-09-15 (**Detalle de `Service`: soporte y renderizado de Footer dinámico (`service.footer.blocks`)** — pedido del Tech Lead: soporte de tipado y renderizado dinámico del footer seleccionado en Studio (`service.footer?.blocks`) con `BlockRenderer.astro` al pie del detalle de servicio en `servicios/[slug].astro`.)
> Antes, 2026-09-14 (**Detalle de `Service`: ancho máximo limitado a 1024px y textos en `cicagray-900` en "¿Por qué elegirnos?" y "Soluciones"** — pedido del Tech Lead: las secciones "¿Por qué elegirnos?" y "Soluciones..." pasan a un ancho máximo estricto de 1024px (`max-w-[1024px]`) alineado con la selección Figma `1024 x 160 adaptar`, y los textos descriptivos pasan a `text-cicagray-900` en `servicios/[slug].astro`.)
> Antes, mismo día (**Detalle de `Service`: rediseño de "¿Por qué elegirnos?" y bloque "Soluciones" con foco-idea y flecha dorada** — pedido del Tech Lead: la sección "¿Por qué elegirnos?" pasa a ser una tarjeta centrada `rounded-2xl` con fondo `bg-cicaindigo-100` (`#E8E4ED`), y el bloque "Soluciones..." se rediseña sin marco gris con el SVG del foco-idea integrado a la izquierda del título, descripción centrada y flecha dorada `ph:caret-down` animada hacia el footer en `servicios/[slug].astro`.)
> Antes, mismo día (**Detalle de `Service`: ampliación de padding horizontal de tabs a `3md:px-10` y `2xl:px-14`** — pedido del Tech Lead: los botones de pestañas pasan a tener `px-8 3md:px-10 2xl:px-14` en `servicios/[slug].astro`, otorgando 40px desde 960/1024px y 56px desde 1440px en adelante.)
> Antes, mismo día (**Detalle de `Service`: agregado de sombra `shadow-sm` en tarjetas del acordeón de "Coberturas"** — pedido del Tech Lead: incorporar la elevación `shadow-sm` a cada tarjeta `<details>` en `servicios/[slug].astro`, logrando la separación y profundidad visual que se observa en la expectativa sobre el fondo `bg-cicagray-50` tanto cerrado como abierto.)
> Antes, mismo día (**Detalle de `Service`: reducción de tamaño de texto en ítems del acordeón (default y activo) en mobile** — pedido del Tech Lead: el título de cada ítem (`coverage.label`) dentro del `<summary>` pasa a tener `text-sm` (14px) en mobile y `md:text-base` (16px) en desktop en `servicios/[slug].astro`, logrando un equilibrio y confort visual óptimo en pantallas pequeñas tanto en estado cerrado como abierto.)
> Antes, mismo día (**Detalle de `Service`: calibración pixel-perfect del acordeón de "Coberturas" (colores default/activo, fondo blanco y decorador dot SVG)** — pedido del Tech Lead: contenedor en `bg-cicagray-50`, items default en `bg-cicaindigo-50`, hover `hover:bg-cicaindigo-100`, active `group-open:bg-cicaindigo-200`, texto activo `text-cicaindigo-500 font-semibold`, checkmark dinámico a índigo `group-open:text-cicaindigo-400`, subcontenido con fondo blanco puro y viñetas personalizadas con el SVG de doble círculo oficial con fill `#A298B8` en `servicios/[slug].astro`.)
> Antes, mismo día (**Detalle de `Service`: reducción de tamaño de texto en viñetas de "¿Qué ofrecemos?" en mobile** — pedido del Tech Lead: el texto de las viñetas pasa a tener `text-sm` (14px) en mobile con `leading-relaxed` frente a `md:text-base` (16px) en desktop en `servicios/[slug].astro`, evitando saltos de línea excesivos y mejorando el confort visual en pantallas pequeñas.)
> Antes, mismo día (**Detalle de `Service`: escala de padding en Y del contenedor de tabs** — pedido del Tech Lead: el contenedor de contenidos (`rounded-2xl`) pasa a tener `py-10` (40px) en mobile, `md:py-[60px]` (60px) en tablet y `3md:py-20` (80px) en desktop (`px-6 md:px-10 py-10 md:py-[60px] 3md:py-20`) en `servicios/[slug].astro`.)
> Antes, mismo día (**Detalle de `Service`: ajuste de padding horizontal de tabs a `px-8`** — pedido del Tech Lead: calibrar el padding lateral de ambos tabs (`¿Qué ofrecemos?` y `Coberturas`) a `px-8` (32px) en `servicios/[slug].astro`, logrando un equilibrio de ancho óptimo en pantallas de cualquier tamaño.)
> Antes, mismo día (**Detalle de `Service`: línea horizontal en border-top del contenedor + padding y texto responsivo en tabs** — pedido del Tech Lead: la línea divisoria se reubicó al `border-t-2 border-cicaindigo-400` del contenedor de contenido (`rounded-2xl`), asegurando que la línea siga las esquinas redondeadas en ambos extremos. En mobile se mantuvo `px-6` (frente a `md:px-9` en desktop) y el texto se ajustó a `text-sm md:text-[0.95rem]`.)
> Antes, mismo día (**Detalle de `Service`: ampliación de padding horizontal de tabs a `px-9`** — pedido del Tech Lead: cambiar el padding lateral de ambos tabs (`¿Qué ofrecemos?` y `Coberturas`) de `px-7` a `px-9` (36px) en `servicios/[slug].astro`, logrando una proporción visual más amplia y equilibrada con las esquinas superiores redondeadas al 100%.)
> Antes, mismo día (**Detalle de `Service`: migración de tabs, acordeón e intro a Tailwind CSS 100% nativo** — pedido del Tech Lead: se eliminó el CSS tradicional del bloque `<style>` para los tabs, acordeones e intro en `servicios/[slug].astro`. Ahora usan directamente utilidades nativas de Tailwind v4: `aria-selected:`, `group-open:`, `rounded-t-full`, `border-cicaindigo-400`, `[&_p]:mb-3`, etc.)
> Antes, mismo día (**Detalle de `Service`: tabs con altura de 40px y esquinas superiores redondeadas al 100%** — pedido del Tech Lead: las pestañas (`[slug].astro`) pasan a tener una altura fija simétrica de 40px (`h-10`, centrado vertical con `inline-flex`) y esquinas superiores redondeadas al 100% (`rounded-t-full`), apoyadas sobre la línea `border-t-2 border-cicaindigo-400` del contenedor inferior `rounded-2xl`.)
> Antes, mismo día (**Detalle de `Service`: reducción de padding inferior a 40px en sección intro** — pedido del Tech Lead: reducir a 40 el padding inferior (`md:pb-10` / `py-8 md:py-10`) en el contenedor de la introducción de `servicios/[slug].astro`.)
> Antes, mismo día (**Detalle de `Service`: reducción del degradado superior en el header a 55% de altura y fade al 20%** — pedido del Tech Lead: reducir la cobertura del degradado oscuro superior en el header a `h-[55%]` y fade al 20% en `servicios/[slug].astro`.)
> Antes, mismo día (**`Service.image_detail` — 2da imagen opcional/panorámica, usada en el header del detalle con fallback a la Principal** — cross-repo (genesis agrega la columna/campo/UX en Console, ver su PROGRESS.md). Acá: `types.ts` (`Service.image_detail: Media | null`, solo en el detalle, no en `ServiceSummary`/catálogo) y `[slug].astro` (`const headerImage = service.image_detail ?? service.image`, usado en el `<img>` de fondo del header — el fallback vive en el frontend, no en el API). `ServicesGrid.astro` (catálogo) sin cambios, sigue usando `image`. Verificado con `@astrojs/compiler` + `tsc --strict` (proyecto completo, sin errores). **Falta migración + reseed en genesis** para que el campo exista en datos reales. Ver PROGRESS.md.)
> Antes, mismo día (**Fix de ancho del detalle de `Service` (`[slug].astro`): TODO el cuerpo (header incluido) topeado a 1200px, banderas alineadas al borde del cuerpo** — 3 pasadas sobre el mismo pedido del Tech Lead, con capturas reales comparando el sitio en vivo contra un mockup de referencia a 1440px/1900px de viewport. 1ra pasada: las 3 secciones bajo el header (párrafo intro, tabs "¿Qué ofrecemos?"/"Coberturas", "¿Por qué elegirnos?"/tip) pasan de `max-w-3xl`/`max-w-4xl` (768/896px) a `mx-auto max-w-[1200px]` — el texto que necesita quedar angosto para legibilidad (párrafo de intro, texto de why_choose_us, el `<aside>` del tip) conserva un `max-w-3xl` interno propio; el `tab-row` gana `justify-center` (antes quedaba pegado a la izquierda al dejar de ocupar todo el ancho del cuerpo). 2da pasada: el Tech Lead marcó que el bloque de texto del HEADER (pretitle/título/subtítulo) se había quedado afuera, seguía en `max-w-3xl` — corregido al mismo `max-w-[1200px]`. 3ra pasada: con captura a ~1900px, las banderas de país flotando sobre el wave quedaban pegadas al borde de la PANTALLA (`absolute right-4/md:right-10` contra la sección full-bleed) en vez de al borde del cuerpo — se envuelven en el mismo `mx-auto max-w-[1200px] px-4 md:px-10` que el bloque de texto, con `justify-end`, para que su borde derecho quede siempre alineado al borde derecho del cuerpo en cualquier ancho de pantalla. `max-w-[1200px]` es un valor arbitrario explícito (no un paso redondeado de Tailwind) — mismo criterio ya documentado en `RichText.astro` para `content_width`, "spec exacta del Tech Lead". Verificado con `@astrojs/compiler` (`parse()`, 0 diagnósticos en las 3 pasadas) + `prettier --write` (sin cambios de formato). `astro build` sigue sin poder correr en este sandbox (native binding de `rolldown` faltante). Pendiente: confirmación visual real del Tech Lead contra el mockup. Ver PROGRESS.md.)
> Antes, mismo día (**Rediseño del header de detalle de `Service` (`[slug].astro`) + `why_choose_us.text` como HTML** — pedido directo del Tech Lead con capturas reales de "Seguros Financiero" en CICA360: header full-bleed ~50vh (configurable Normal/Destacado ×1.25 vía `properties.header_type` de genesis), degradado al 50% de SU altura, decorador wave, banderas de país flotando sobre el wave (`properties.show_decorative_detail` los apaga como conjunto), párrafo intro con el mismo look que `RichText.astro` (flecha `ph:caret-down`), tabs "¿Qué ofrecemos?"/"Coberturas" sobrepuestas `-mt-10`, íconos `ph:check-circle-fill` consistentes en bullets y acordeón. Reusa el path SVG del wave y el estilo de banderas ya establecidos en `RichText.astro`/`ServicesGrid.astro` — nada inventado de cero. `why_choose_us.text` ahora llega como HTML ya renderizado (Console: `Textarea` → `RichEditor`) — el front usa `set:html` en vez de interpolación `{}`. Verificado con `@astrojs/compiler` (`parse()`, 0 diagnósticos) + `tsc --noEmit --strict` (sin errores) — `astro check`/`build` no pudieron correr en este sandbox (native binding de `rolldown` faltante, no relacionado al código). Ver PROGRESS.md (acá y en genesis).)
> Antes, 2026-09-13 (**Deploy a producción: manual vía cPanel (zip del artifact `cica360-dist`), no FTP** — decisión explícita del Tech Lead ante la ventana de pre-lanzamiento con contenido cambiando seguido. El artifact de `actions/upload-artifact@v4` ya produce el zip listo para subir (archivos en la raíz, sin carpeta `dist/` envolvente). `.env` de los proxies PHP vive un nivel por encima del document root a propósito (`_env.php`) — nunca lo toca la extracción del zip. `ARCHITECTURE.md §6` actualizado con el diagrama real. Ver PROGRESS.md.)
> Antes, mismo día (**Ronda de performance (Lighthouse/PageSpeed): robots.txt inválido + Google Fonts render-blocking** — `public/robots.txt` estático → `src/pages/robots.txt.ts` (URL de `Sitemap:` absoluta vía `Astro.site`, antes relativa e inválida). `BaseLayout.astro`: Google Fonts pasa de `<link rel="stylesheet">` síncrono a patrón "preload + swap" (sin librerías nuevas) — ya no bloquea el render (~450ms). Self-hospedar Lato (`@fontsource/lato`) queda pendiente, no se pudo instalar por falta de red en este sandbox. Ver PROGRESS.md (acá y en genesis, que sumó cache headers para media en la misma ronda).)
> Antes, mismo día (**Meta Pixel / Google Tag Manager: inyección real con carga perezosa (ver genesis ADR-066)** — `getSiteTracking()` nuevo en `api.ts` (`SiteTracking` en `types.ts`), consumido una vez por página en `BaseLayout.astro`. Si el tenant cargó algún ID en Preferencias > Integraciones (genesis), se inyecta un único `<script define:vars={{ gtmId, metaPixelId }} is:inline>` con el boilerplate oficial de cada proveedor + `<noscript>` fallback — pero diferido hasta la primera interacción real del visitante o un timeout de 5s de respaldo, para no afectar TBT/INP/Lighthouse. Se descartó `@astrojs/partytown` por sobre-ingeniería. Ver PROGRESS.md.)
> Antes, mismo día (**Fix real: comentarios internos filtrándose al `<head>` público + SEO/OG del backend nunca se renderizaba en el sitio** — reporte del Tech Lead viendo "Ver código fuente" en vivo. `BaseLayout.astro` tenía 4 bloques `<!-- -->` con contexto interno (ADRs, decisiones de proceso) DENTRO del `<head>` renderizado — Astro los deja pasar tal cual al HTML final; se migraron al frontmatter (invisible en el output). Además, `BaseLayout` solo aceptaba `title`/`description`: aunque genesis ya resuelve `meta.seo_title`/`seo_keywords`/`og_title`/`og_description`/`og_image_rect`/`og_image_square` con fallback de tenant (ver genesis ADR-065), ningún `src/pages/*.astro` se lo pasaba — nunca se veía nada más allá de `<meta name="description">`. `BaseLayout` gana props `seoTitle`/`keywords`/`ogTitle`/`ogDescription`/`ogImage` + tags `og:*`/`twitter:*` reales; los 4 consumidores (`[slug].astro`, `index.astro`, `blog/[slug].astro`, `servicios/[slug].astro`) los pasan desde `meta`. `PageMeta` (`types.ts`) tipado explícito para los campos nuevos. Ver PROGRESS.md.)
> Antes, 2026-09-11 (**`ContactForm.tsx` rediseñado completo contra el mockup real de "Contactame"** — "la espectativa de contactame, quiero que se genere el formulario basico y que envie al endpoint correcto". 7 campos (antes 4), layout 2 columnas, selects de País (8 opciones)/Área de interés (9 servicios reales), botón pill dorado "Contactame ahora" con ícono de avión de papel. `ContactFormPayload` (`types.ts`) gana `city`/`country`/`area_of_interest`. `ContactFormBlock.astro`: contenedor `max-w-xl`→`max-w-3xl`. Envío/endpoint sin cambios (`submitContactForm()` → proxy `contacto.php` → `POST /forms/contacto/submit`). Coordinado con genesis (mismo día): `FormFieldDefinitionSeeder`/`Cliente0ContentSeeder::upsertContactForm()` siembran los 3 campos nuevos + sus `options`. Pendiente `db:seed` en genesis + confirmación visual — ver PROGRESS.md.)
> Antes, mismo día (**`TestimonialsGrid.astro` nuevo — catálogo de "Casos de éxito" (bloque `testimonials_grid`, mismo tratamiento que `services_grid`)** — "es un bloque de testimonios que creamos a modo preview o resumen solo para home u otras paginas, pero este tiene que ser un bloque nuevo especial como el de servicios". Coordinado con genesis (ver ADR-050): el bloque nuevo se resuelve en runtime igual que `services_grid`, comparte query con el bloque `testimonials` (teaser) pero ordena por `sort_order`. Componente nuevo: misma estructura de sección que `ServicesGrid.astro` (grid estático, sin carousel, "Más casos" de a 6 client-side), tarjeta reusa el diseño de `Testimonials.astro` (avatar circular, frase itálica, firma), sin chips de filtro. `BlockRenderer.astro`/`types.ts` actualizados. Pendiente `db:seed` en genesis + confirmación visual — ver PROGRESS.md.)
> Antes, mismo día (**Ronda de pulido de `ServicesGrid.astro`**: fix real de `cursor-pointer` con parpadeo — `pointer-events: none` global en íconos/imágenes dentro de `button`/`a` (`global.css`); carga inicial 9→6 y "Ver más servicios"→"Más servicios" con ícono de ojo; `cursor-pointer` en chips de filtro y botón; padding-top de la sección −20px; ícono de bandera de los chips a 28px en mobile/20px desde `sm`; chip "Todos" `py-2`→`py-1`. Ver PROGRESS.md para el detalle línea por línea de cada pedido puntual.)
> Antes, mismo día (**`ServicesGrid.astro`: alineación centrada, botón primary, banderitas como ícono real sobre el decorador de la ola** — 3 correcciones puntuales comparando mockup vs. realidad ("titulos, breve descripcion y boton... alineados al centro"; "el boton es tipo o estilo principal"; "las banderitas... bajar y posicionarse a la altura del decorador... con iconos"). Banderitas ahora `<img>` real, hermanas del `<svg>` de la ola (ya no embebidas en el div de imagen con `overflow-hidden`), `flag_GENERAL.webp` de fallback para "Regional/Global". 9 archivos de banderas copiados de `sources/flags/` a `public/flags/` (el API no los devuelve, es un asset del frontend). El shape `countries: [{iso, name}]` que el Tech Lead pidió como "paises: {pais_iso, pais_name}" YA EXISTE en genesis (ADR-049) con nombres de campo en inglés — avisado, no renombrado, a la espera de confirmación. Pendiente confirmación visual — ver PROGRESS.md.)
> Antes, mismo día (**`ServicesGrid.astro` rediseñado por completo: grid estático (NO carrusel) + "Ver más servicios" de a 9 (100% client-side, sitio estático) + chips de filtro por país.** Coordinado con genesis: `services_grid` deja de traer `content.items` manual, se resuelve en runtime contra la tabla real `services` — mismo patrón que `testimonials` (ver ADR-049 en genesis). Correcciones clave del Tech Lead a mitad de implementación: "testimonials usa slides o sliders y eso no es el caso, aquí es modo grid" (se descartó cualquier carrusel/GSAP) y "la estructura de sección... como los demás existentes que usa el partial heading" (sección estándar + `BlockHeading.astro`, igual que `Features`/`RichText`/`Logos`). Países como badge de texto (sin ícono de bandera, no existe ese asset). Pendiente: `db:seed` en genesis (`Cliente0ServicesSeeder`, 12 servicios), `npm run build` + confirmación visual del Tech Lead, pasada de diseño fina (Antigravity/Gemini) — ver PROGRESS.md.)
> Antes, mismo día (**`Hero.astro` — ajuste "solo tablet grandes" (`3md`–`4md`, 960-1079px)**: "solo un ajuste... solo para las resoluciones de tablet 3md: y 4md:". Padding: div exterior `py-[150px]`→`3md:py-[240px]`+`lg:py-[150px]` reset; div anidado `px-[100px]`→`3md:px-[150px]`+`lg:px-[100px]` reset (`3xl:px-[200px]` sin tocar). Tamaños (todos con reset a `lg` para no filtrar a desktop): pretitle 14→16px, título agrega `3md:text-[56px]` (el `lg:text-hero` ya existente lo resetea solo), subtítulo 20→24px, CTA (alto/padding-x/texto/ícono) un paso arriba. NO implementado: "description, 2 o 3 puntos" — el modelo de datos del slide no tiene esos campos, no se inventaron; pendiente aclarar con el Tech Lead. Pendiente confirmación visual en 960-1079px** — ver PROGRESS.md.)
> Antes, mismo día (**`cursor-pointer` sitewide en 7 `<button>` que no lo tenían**: "en los enlaces del navbar y donde exista un enlace o action deberia de mostrarse el cursor-pointer". Causa real: `<a href>` ya recibe pointer por default (sin reset en `global.css`, Tailwind v4 Preflight no lo toca), pero `<button>` NUNCA lo recibe por default en ningún navegador. Auditado TODO el proyecto — corregidos: `Header.astro` (`linkBaseClass`/`ctaBaseClass`/CTA del overlay mobile — explícito también en los `<a>`, redundante pero consistente), `Hero.astro` (prev/next/dots del carousel), `Logos.astro` (prev/next del carousel), `ContactForm.tsx` (botón de envío + `disabled:cursor-not-allowed`). `Testimonials`/`Features` no tienen `<button>`, sin cambios. Pendiente confirmación visual** — ver PROGRESS.md.)
> Antes, mismo día (**Nuevo partial compartido `src/components/blocks/BlockHeading.astro`**: "todos los headings deberian ser... un solo partial component reutilizado". Migrados `RichText`/`Testimonials`/`Logos`/`Features` (los 4 que ya compartían la estructura exacta). Auditoría reveló 2 criterios de color conviviendo (explícito vs. opacidad) — el Tech Lead pidió color explícito, sin opacidad, por prop. Cambios visuales reales: `Logos` (opacity→color de marca), `Features` (pretitle opacity→gold, corrige inconsistencia interna), `Testimonials` (pierde el matiz de opacidad en pretitle/subtítulo, NO recibe colores de marca por su fondo teal oscuro propio — hubiera roto el contraste). `Split`/`Cta` deliberadamente sin tocar ("lo vemos despues") — no tienen wrapper `gap-1` propio hoy. Pendiente confirmación visual, sobre todo Logos y Testimonials** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — flecha de scroll pasa a hardcodeada, sin gate de property (12va vuelta): "implementar hardcode entonces". Se quita `properties.show_scroll_indicator &&` del markup — se renderiza siempre. La property/campo de Filament quedan declarados sin consumidor, por si se retoma control por bloque más adelante. Pendiente confirmación visual** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — flecha de invitación a scrollear agregada por primera vez (11va vuelta): "no veo hasta ahora la flecha de invitacion a scrollear que ya usa otros bloques que ya hemos hecho". Mismo patrón que `RichText.astro` (`ph:caret-down` + `animate-bounce`, `text-cicagold-500`, en flujo normal `mt-3`, no overlay). `FeaturesProperties` gana `show_scroll_indicator`, se importa `Icon` de `astro-icon`. Gap de backend cerrado de paso: el campo NO existía en el schema de Filament del bloque `features` (genesis, `PageResource.php`) — agregado `PropertiesSchema::make(['show_scroll_indicator'])`, campo reusable sin setup adicional. Pendiente: activar el toggle en el admin y confirmar visualmente** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — gap de la sub-lista (`item.items`, ej. "Valores") reducido a gap-1 (10ma vuelta): "si hay sub lista que la lista tenga gap-1". `gap-2` (8px) → `gap-1` (4px) SOLO en la variante `flex flex-col`; la variante `grid` sin cambios. Pendiente confirmación visual** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — padding-left del área de texto sube a 32px en el stack de tablet (9na vuelta): "el padding left del area de texto cambair de px-6 a px-8" (confirmado: "perdon pl-8", luego "esta bien px-8" validando el valor de 32px). Contenedor de contenido (`isBoxed`): `md:p-5` → `md:py-5 md:pr-5 md:pl-8` — solo el lado izquierdo sube a 32px, arriba/abajo/derecha quedan en 20px. Solo aplica a `boxed_*` (la captura, Misión/Visión/Valores); `simple`/`shapes` no tiene padding propio ahí. Pendiente confirmación visual** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — altura mínima de 180px por fila en el stack de tablet (8va vuelta): "cada elemento que tenga minimo 180px de altura". `md:min-h-[180px]` en el wrapper del item (imagen y contenido respetan el mismo piso), reseteado con `4md:min-h-0` para no afectar el grid de desktop. Pendiente confirmación visual** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — gap entre filas del stack de tablet, un paso más abajo (7ma vuelta): "un poco menos de row gap". `md:gap-6` (24px) → `md:gap-5` (20px, el gutter estándar del bloque). `4md:gap-8` (grid) sin cambios. Pendiente confirmación visual** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — gap entre filas del stack de tablet reducido (6ta vuelta): "el gap entre elementos reducir un poco". `md:gap-8` (32px) → `md:gap-6` (24px) SOLO en 768-1023px; el grid (`4md`+/1024px) conserva `gap-8` (32px) explícito vía `4md:gap-8`. Pendiente confirmación visual** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — padding-x del stack de tablet corregido (5ta vuelta), bug real: "se pega a los bordes laterales" conforme se reduce desde 1023px. `DESKTOP_CONTENT_WIDTH_CLASSES` solo aportaba padding desde `3md`/960px — quedaba un hueco sin padding entre `md`/768px y `3md`/960px. Fix: `MOBILE_CONTENT_WIDTH_CLASS` gana `md:px-5` (20px). De paso se corrigió un bug de doble padding en el heading (su inset propio se apagaba en `3md:px-0`, ahora en `md:px-0`, para no sumarse con el nuevo padding del padre). Pendiente confirmación visual** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — gap heading→cards reducido en el stack de tablet (4ta vuelta): "mucha separacion entre heading y elementos". El wrapper `gap-6 xs:gap-12` sube a `gap-12`/48px desde 414px en TODO el rango — se sentía excesivo en el stack de tablet nuevo (768-1023px). Fix: `md:gap-8 4md:gap-12` — 32px SOLO en 768-1023px (coincide con el token de Figma de este mismo rediseño), vuelve a `gap-12`/48px desde `4md`/1024px (grid). Base/mobile sin cambios. Pendiente confirmación visual** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — `.features-viewport` ya no recorta las sombras del stack de tablet (3ra vuelta): "no se ven las sombras laterales y del ultimo elemento, como que el viewport está con overflow hidden... pero impiden los shadows". El wrapper de clipping tenía su `overflow-visible` recién desde `4md`/1024px (arranque del grid) — pero el stack de tablet (768-1023px) tampoco tiene scroll horizontal/GSAP (el pin solo corre <768px) y heredaba el recorte igual, cortando el `shadow-lg` de las tarjetas. Umbral baja a `md`/768px (coincide con el umbral real de GSAP, no con el del grid). Pendiente confirmación visual** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — corrección del umbral del grid, 2da vuelta del cambio de estrategia: de `lg`/1080px a `4md`/1024px. El Tech Lead reconfirmó su pedido original ("te dije desde md: (768px) y menor igual que 3md: (1024px)", luego "desde 1024px a más es normal de grid 3 col") — 1024px se llama `4md` en la escala custom de este proyecto (`3md` es 960px, no 1024px; se aclara la nomenclatura para no repetir la confusión). Todos los `lg:` de la vuelta anterior (`CARD_CLASSES`, track, `.features-viewport`, contenedores de imagen/contenido) pasan a `4md:`. Stack de tablet ahora cubre 768-1023px (antes 768-1079px); grid arranca en 1024px (antes 1080px). Umbral de GSAP/pin (767.98px) sin cambios. `content_width` sigue sin alinear (`3md`/960px) — mismo efecto secundario conocido, ahora el hueco de desalineo es 960-1024px en vez de 960-1080px. Pendiente confirmación visual en los 3 rangos** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — cambio de estrategia completo: 3 layouts por breakpoint, mismo markup. Base (<768px): sin cambios, slider de a 1 con GSAP pin. `md`-`lg` (768-1079px, luego corregido a `4md`/1024px, ver entrada de arriba): stack vertical de 1 columna, cada item en fila horizontal (imagen ~40% izquierda, texto centrado a la derecha), sin JS/pin/snap. `lg`+ (1080px+, luego corregido a `4md`/1024px+): grid de 3 columnas (antes arrancaba en `3md`/960px). Umbrales de JS (`isCarouselViewport`, `gsap.matchMedia`) y del CSS `.features-last-item` bajados de 959.98px a 767.98px. Valores de la fila de tablet (imagen 40%, `p-5`, `gap-8`) interpretados de capturas de Figma sin spec exportado — no 100% pixel-perfect garantizado. Pendiente confirmación visual en los 3 rangos** — ver PROGRESS.md.)
> Antes, mismo día (**`Heading.astro` — altura del banner en 768-1024px, valor final: "creo que vamos con 25vh". `md:min-h-[25vh]` (antes `md:min-h-[40vh]` fijo hasta `lg`/1080px; un 1er intento con `36vh`/`4md:34vh` se reemplazó por este valor único). `lg`/1080px en adelante sin cambios (`34vh`). Curva final: 34vh(base)→30vh(sm)→25vh(md/768-1079)→34vh(lg/1080). Pendiente confirmación visual** — ver PROGRESS.md.)
> Antes, mismo día (**Interlineado (leading) reducido en <414px, en TODOS los blocks con heading** (pedido explícito sitewide): pretitle/título bajan a `leading-tight` (1.25), subtítulo a `leading-snug` (1.375), por debajo de `xs` (414px) — desde ahí vuelven al interlineado normal. Tocados: `RichText.astro`, `Split.astro` (constantes compartidas), `Testimonials.astro`, `Logos.astro`, `Features.astro`, `Cta.astro` (solo pretitle/título — su `subtitle` es la "descripción breve" con `leading-8` fijo, caso aparte, no se tocó). Pendiente confirmación visual** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — el carousel pineado (mobile) ahora asienta en una tarjeta completa al soltar el scroll: se agrega `snap` a `ScrollTrigger.create()` (nueva función `snapToNearestCard`) — antes, con `scrub: 1` continuo y `scroll-snap-type: none` durante el pin, el carousel se quedaba en CUALQUIER posición intermedia al soltar, mostrando fracciones de 2-3 tarjetas ("empujado hacia abajo"); ahora GSAP anima hacia la tarjeta más cercana automáticamente. Pendiente confirmación visual** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — centrado vertical de vuelta: `align-items: flex-start` → `align-items: center` en `.js-pin-active`. El `overflow: hidden` agregado en la vuelta anterior ya alcanzaba solo para evitar la invasión del bloque anterior (recorta en cualquier dirección, no depende de `align-items`) — `flex-start` era una protección extra innecesaria que dejaba el heading "muy arriba" en la mayoría de los celulares. Pendiente confirmación visual** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — gap heading→carousel reducido SOLO por debajo de 414px (`xs`, breakpoint ya definido): `gap-12` → `gap-6 xs:gap-12` — 24px en la base, vuelve a 48px (el valor compartido con `Testimonials`/`Logos`) desde 414px+. Pendiente confirmación visual** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — "no hay espacio arriba" resuelto SIN tocar la altura del elemento pineado (lección de la vuelta anterior): `align-items: center` → `align-items: flex-start` en `.js-pin-active` — el contenido arranca siempre pegado al padding superior real, nunca corrido por el centrado, sin cambiar la altura de la sección (no repite el bug del spacer de GSAP). `overflow: hidden` agregado como red de seguridad para celulares muy bajos. Contrapartida aceptada: puede quedar aire de más abajo en vez de repartido arriba/abajo. Pendiente confirmación visual** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — REVERT del fix de `js-pin-active`/`onToggle` de la vuelta anterior: rompía el pin de GSAP ("el contenido sigue firme... como si estuviera escalado en zoom") — `ScrollTrigger` con `pin: true` no tolera que el elemento pineado cambie de altura por su cuenta mientras el pin está activo (spacer interno se desincroniza). Vuelve a `section.classList.add('js-pin-active')` permanente, sin `onToggle`. El problema original ("no hay espacio arriba") queda SIN RESOLVER — cualquier fix futuro no puede depender de cambiar el alto de un elemento pineado en caliente. El resto de fixes de hoy (padding-x del heading, escala H2, gap/px del carousel <390px) NO se tocaron, siguen vigentes** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — el carousel gana un escalón más angosto SOLO por debajo de 390px (`2xs`, breakpoint ya definido en `global.css`): "no se lee completo los textos, reducir mas el gap y padding en x" — `gap-6`/`px-5` (24/20px) bajan a `gap-4`/`px-4` (16px) en la base, vuelven a los valores actuales desde `2xs` (390px+). El "peek" (`basis-[75%]`) no se tocó, sigue deliberado. Ningún otro rango (390-959px) afectado. Pendiente confirmación visual** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — heading (título/subtítulo/gap) alineado a la escala H2 compartida del sitio (`RichText`/`Split`/`Testimonials`/`Logos`/`Cta`): "por que el heading de esta seccion no esta alineado con las demas secciones que ya hemos definido estandar... las demas tienen lo mismo y esta bien definido no se debe tocar". Título pasa de `text-3xl sm:text-4xl` a `text-2xl sm:text-3xl 4md:text-4xl` (escala compartida real); subtítulo de `text-lg opacity-80` fijo (+ `max-w-3xl` extra) a `text-lg md:text-base text-gray-500` (mismo color que usan los bloques de fondo claro). De paso corrige un error de la vuelta anterior: el gap heading→carousel había bajado a `gap-6` copiando el criterio equivocado (`RichText`, heading→párrafo) — vuelve a `gap-12`, el valor real que comparten `Testimonials`/`Logos` (heading→carousel de tarjetas, la categoría correcta de comparación). Pendiente confirmación visual** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — BUG REAL encontrado y corregido: "no hay espacio arriba" (reportado con 3 capturas <540px, reproducible EN REPOSO, sin necesitar ningún instante exacto de scroll). Causa: `section.classList.add('js-pin-active')` corría UNA vez al cargar la página (dentro de `gsap.matchMedia`), no en cada evento de scroll — la sección quedaba SIEMPRE a `100vh` + centrada verticalmente en cualquier viewport <960px, sin importar la posición real del scroll, contradiciendo el pedido original ("...SOLO cuando se activa el scroll horizontal"). Con contenido más alto que el espacio libre bajo el navbar en algunos casos, el centrado empujaba el bloque hacia arriba, comiéndose el padding superior e invadiendo el bloque anterior. Fix: la clase se alterna en vivo vía `onToggle` del `ScrollTrigger` (`self.isActive`) — activa solo mientras el pin está genuinamente enganchado, alto natural el resto del tiempo. No se tocó `js-scroll-pin` (track) ni el fix estructural de "100vh mientras el pin está activo" (3ra vuelta) — solo CUÁNDO se aplica. Pendiente confirmación visual, no requiere capturar ningún instante puntual** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — ejecutado el fix confirmado del análisis anterior ("procede ahora"): el heading (pretitle/título/subtítulo) pasa de `px-5` fijo (20px en todo <960px) a `px-4 sm:px-6` (16px→24px), el mismo patrón escalonado de 3 pasos que usa `RichText.astro`/bloques "boxed". El `px-5` del carousel TRACK (elemento distinto, atado a `distance()` de GSAP y `snap-end`) NO se tocó** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — ANÁLISIS (ya ejecutado, ver entrada de arriba): "no hay espacio arriba" + "no mantiene el standar de padding en x minimo proporcional a cada resolucion" en <540px. Hallazgo confirmado por código: el heading usaba `px-5` fijo en todo <960px, sin escalonar como `RichText.astro` (`px-4 sm:px-6 lg:px-8`). Hipótesis sin confirmar sobre el hueco vertical: posible diferencia entre el estado pineado (`js-pin-active`, harto aire) y el momento de transición justo antes del pin (solo `py-12` natural, se ve corto) — pendiente verificar con captura de DevTools en ese instante exacto** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro` — REVERT del cambio de la vuelta anterior: quitar `height:100vh`/`100dvh` de `.js-pin-active` causó un bug real confirmado con capturas ("fondo blanco... la seccion verde siguiente" se asomaba prematuramente durante el scroll horizontal) — la duración del pin de GSAP depende de `distance()` (ancho horizontal), no del alto de la sección, así que una sección más baja que el viewport deja un hueco por el que la sección siguiente se ve antes de tiempo. El `100vh`/`100dvh` no era cosmético, evitaba justo ese artefacto — no existe una altura reducida que lo evite sin cubrir el 100% del viewport. Se revierte por completo. Pendiente: conversar con el Tech Lead una alternativa distinta para su pedido original de tablet (no "achicar el número")** — ver PROGRESS.md.)
> Antes, 2026-09-08 (**`Features.astro` — se quita el `height: 100vh`/`100dvh` forzado de la sección pineada (`.js-pin-active`) para todas las resoluciones <960px: "reducir la altura... que no sea fullheight... el resto no cambiar igual que siga la interaccion". La sección vuelve a medir su alto natural según el contenido — confirmado que la mecánica del pin/scroll-jack (GSAP ScrollTrigger) depende de `distance()` (ancho a recorrer), no del alto de la sección, así que no se afecta esa interacción. Sin valor numérico específico pedido, se interpretó como alto automático. Este cambio resultó tener un bug real, ver entrada de arriba (REVERTIDO)** — ver PROGRESS.md.)
> Antes, mismo día (**`Logos.astro` — se quita el wraparound infinito (mismo bug que `Testimonials.astro`, confirmado y corregido: clonaba los primeros logos y los pegaba al final del track, con navegación dando la vuelta infinita). Se elimina la clonación, `scrollToIndex()` pasa a CLAMP en vez de wraparound, auto-avance se detiene al llegar al final. Sin necesidad del clamp físico al máximo scroll de `Testimonials.astro` (acá no hay márgenes/gutters especiales en juego). Pendiente confirmar en vivo** — ver PROGRESS.md.)
> Antes, mismo día (**`Logos.astro` + `Hero.astro` — mismo fix de dots del carousel de `Testimonials.astro` extendido a `Logos.astro` (`handleScrollSettled()`, sincroniza el dot con `nearestPage()` cuando el scroll se asienta). `Hero.astro` gana soporte de swipe por primera vez (no tenía NADA de gesto táctil antes, solo botones/dots) — swipe por umbral (50px) con Pointer Events + direction-locking, sin arrastre visual 1:1 (el slider es crossfade por opacidad, no un track scrolleable, así que no aplica el mismo mecanismo). Pendiente confirmar en vivo, sobre todo el swipe del Hero por ser funcionalidad nueva** — ver PROGRESS.md.)
> Antes, mismo día (**`Testimonials.astro`, 23va vuelta — los dots no seguían un swipe/drag manual: "el indicador dots no se actualiza... luego sigue su curso y marca otra cosa". El dot solo se actualizaba en 3 sitios puntuales (click en dot, `onPointerUp`, auto-avance) — un scroll que terminara por otra vía (momentum nativo, trackpad/wheel) nunca lo actualizaba, y el auto-avance retomaba desde un `current` viejo. Fix: nuevo listener `handleScrollSettled()` en el evento `scroll` del track — con cada evento se reprograma un timeout de 120ms; al asentarse el scroll, sincroniza el dot con `nearestPage()`, sin forzar ningún re-snap. Pendiente confirmar en vivo** — ver PROGRESS.md.)
> Antes, mismo día (**`Testimonials.astro`, 22va vuelta — fix real y definitivo (7mo intento) del hueco izquierdo de la última página: se abandona el `margin-left` por completo (nunca funciona de forma confiable en un item que no es el primero del track — el navegador re-centra cada tarjeta vía `scroll-snap-type: mandatory`, sin importar el destino calculado). Se usa la MISMA técnica ya confirmada para `item[1]` (16va vuelta): reducir ANCHO, no margen — pero aplicada al ÚLTIMO item real (`index === items.length - 1`), no al penúltimo. La cuenta cierra exacta: penúltimo (ancho estándar) + gap + último (reducido, `calc(50%-30px)`) + `pr-5` del track = 100% del viewport — el penúltimo queda flush a la izquierda automáticamente. Se elimina la constante `lastPageFirstRealIndex` (ya sin uso). Pendiente confirmar en vivo** — ver PROGRESS.md.)
> Antes, mismo día (**`Testimonials.astro`, 21va vuelta — BUG REAL encontrado con evidencia de DevTools del Tech Lead: `items.length = 5` (impar), y `lastPageFirstRealIndex` (fórmula par/impar de la 15va vuelta, artefacto del wraparound ya removido) calculaba mal el índice — aplicaba `sm:ml-5` al ÚLTIMO real ("Sofía R.", 5to) en vez de al PENÚLTIMO ("Gonzalo S.", 4to, el que visualmente es PRIMERO en la última página física tras el clamp de la 20va vuelta). Resultado: el margen se apilaba ENTRE Gonzalo y Sofía (gap "duplicado"), no antes de Gonzalo. Fix: fórmula simplificada a `Math.max(0, items.length - 2)`, sin distinción par/impar — con el clamp físico al máximo scroll, el primer item de la última página visible es siempre `N-2`. Pendiente confirmar en vivo, pero diagnosticado con DOM real, no por deducción — alta confianza** — ver PROGRESS.md.)
> Antes, mismo día (**`Testimonials.astro`, 20va vuelta — fix real (5to intento) del margen izquierdo de la última página: las vueltas 16–18 calculaban un destino de scroll aproximado (`offsetLeft - 20px`, luego `snap-start`, luego `scroll-margin-left`) que nunca coincidía con el límite real (siempre sobraba el `pr-5` final, el navegador tenía margen para "corregir" lejos del destino). Fix: `scrollToIndex()`/`nearestPage()` apuntan directo a `scrollWidth - clientWidth` (el máximo físico de scroll) para la última página — sin "más allá" a donde corregir, mismo principio por el que el `ml-5` de la página 0 (`scrollLeft: 0`) siempre funcionó. Item vuelve a `snap-center` default, sin `snap-start`/`scroll-margin-left`. Pendiente confirmar en vivo** — ver PROGRESS.md.)
> Antes, mismo día (**`Testimonials.astro`, 19na vuelta — se quita el wraparound infinito: "corrige... la cantidad configurada en el admin (5) pero me estás devolviendo 5" resultó ser el loop de clones al final del track (el backend SÍ respeta `content.limit`, confirmado) — al seguir scrolleando después del último testimonio real, el carousel mostraba de nuevo los primeros, percibido como duplicación. Decisión del Tech Lead: quitar el loop, detener en el último. Se elimina la clonación (`cloneNode`/`clones[]`) por completo, `scrollToIndex()` pasa de wraparound (`% totalPages`) a CLAMP, y el auto-avance se detiene solo al llegar al final en vez de reiniciar. La última página, si queda incompleta, se ve tal cual (sin autocompletar). El hack de margen del borde izquierdo de la última página (vueltas 15–18, aún sin resolver) se dejó intacto por pedido explícito ("sigue igual pero dejemoslo") — pendiente evaluar si sin wraparound deja de hacer falta** — ver PROGRESS.md.)
> Antes, mismo día (**`Testimonials.astro`, 18va vuelta — fix real de la 17va, que tampoco alcanzó ("al final sigue sin espacio o margen a la izquierda"): `scroll-snap-align` alinea el ÁREA DE SNAP del elemento, que por spec de CSS es su padding-box y NO incluye el `margin` — el `snap-start` de la 17va alineaba el borde de la TARJETA (sin su `ml-5` de 20px) contra el viewport, el margen seguía quedando fuera de vista. Fix: se agrega `sm:scroll-ml-5 lg:scroll-ml-0` (`scroll-margin-left`, propiedad DISTINTA de `margin-left`, existe justo para este caso) del mismo valor que el margen real, para que el punto de snap se detenga al INICIO del margen. Clones stripean la nueva clase, mismo riesgo de siempre. Pendiente confirmar visualmente — 4to intento sobre este mismo punto. El otro reporte del Tech Lead, "en medio de la lista, se presenta sobre gap", queda sin diagnosticar — ambiguo, no se tocó nada al respecto** — ver PROGRESS.md.)
> Antes, mismo día (**`Testimonials.astro`, 17va vuelta — fix real del borde izquierdo de la última página (item[1]/borde derecho ya confirmado OK): el `snap-center` nativo de TODAS las tarjetas + `scroll-snap-type: x mandatory` corregía la posición final hacia el centro de la tarjeta, deshaciendo el offset de -20px de `scrollToIndex()`/`nearestPage()` (16va vuelta) — a diferencia de la página 0, la última NO está en el límite máximo de scroll, así que sí es corregible por el navegador. Fix: el primer item de la última página cambia su anclaje de `snap-center` a `snap-start` (`sm:snap-start lg:snap-center`, mismo truco ya usado en `Features.astro` con `snap-end`) — el navegador alinea su borde izquierdo (con margen incluido) contra el viewport, sin pelear contra ninguna corrección nativa** — ver PROGRESS.md.)
> Antes, mismo día (**`Testimonials.astro`, 16va vuelta — fix real de la 15va, que no tuvo efecto ("se mantiene, sigue igual"): el margen no puede crear espacio visible antes del borde cuando hay más contenido real después (el margen se "pierde" fuera de pantalla) ni cuando el propio cálculo de scroll usa el `offsetLeft` YA marginado como destino (se auto-cancela). Fix: borde derecho de la 1ra página (item[1]) ahora usa ANCHO reducido (`sm:basis-[calc(50%-30px)]`, no margen); borde izquierdo de la última página mantiene el margen (`sm:ml-5`) PERO `scrollToIndex()`/`nearestPage()` ahora restan 20px al destino cuando es la última página y `itemsPerView===2`, para que el margen quede visible dentro del viewport en vez de scrolleado de más** — ver PROGRESS.md.)
> Antes, mismo día (**`Testimonials.astro`, 15va vuelta — margen en los bordes de la 1ra y última página en `sm`–`lg` (540–1079px): `item[1]` (2do de la 1ra página, fijo) gana `sm:mr-5 lg:mr-0`; el primer item real de la última página gana `sm:ml-5 lg:ml-0` (índice calculado en build-time según paridad de `items.length` — nueva constante `lastPageFirstRealIndex`). Páginas intermedias sin cambios (confirmado por el Tech Lead que ya están bien). Clones del wraparound stripean los nuevos márgenes por el mismo riesgo ya conocido en este archivo. Enfoque confirmado con el Tech Lead antes de tocar código** — ver PROGRESS.md.)
> Antes, mismo día (**`Testimonials.astro`, 14va vuelta — gap del carousel en `sm`–`lg` sube de 10px a 20px: `sm:gap-2.5`→`sm:gap-5`. Aplicando la lección de la 10ma vuelta, el `basis` de cada tarjeta también se actualiza en la misma proporción: `sm:basis-[calc(50%-5px)]`→`sm:basis-[calc(50%-10px)]` (mitad del gap real) para no dejar un espacio "fantasma"** — ver PROGRESS.md.)
> Antes, mismo día (**`Testimonials.astro`, 13va vuelta — REVERT completo de la 11va/12va: "se pudrio todo, te confundiste... tiene que ser de 2 en 2 y centrados". Esas 2 vueltas habían reinterpretado 2 mensajes ambiguos como pedido de rediseño (de "2 tarjetas" a "1 activa + peek") — interpretación incorrecta, el spec original de 2026-09-02 ("1 en 1, de 2 en 2 y 3 en 3") sigue vigente sin cambios. Se revierte quirúrgicamente: `data-testimonials-track` sin `sm:px-[25%]`, primer item sin condicionales de breakpoint (`ml-5` fijo), `measureItemsPerView()` de vuelta a `Math.round`. Grep confirma las 3 piezas funcionales idénticas a la 10ma vuelta** — ver PROGRESS.md.)
> Antes, mismo día (**`Testimonials.astro`, 11va/12va vuelta — el rango `sm`–`lg` (540–1079px) pasa de "2 tarjetas completas" a "1 activa centrada + peek de la siguiente", en 3 mensajes seguidos del Tech Lead: "deberian verse... 1 con la mitad de relleno del siguiente testimonio" + "falta centrar mejor y pasar de 1 en 1 al centro" + "que inicie el primer elemento al centro, asi como el ultimo termine al centro". El ancho de tarjeta (~50%) ya era correcto; lo que fallaba: (1) `measureItemsPerView()` usaba `Math.round` en vez de `Math.floor`, tratando el ~1.9 como 2 tarjetas y navegando de a 2 en vez de 1 — fix real; (2) primer/último item no tenían espacio para centrarse como uno del medio — fix: `sm:px-[25%]` real (no solo scroll-padding) en el track, reemplazando el `ml-5`/`pr-5` fijo SOLO en ese rango (`lg:px-0 lg:pr-5` restaura el comportamiento original de escritorio)** — ver PROGRESS.md.)
> Antes, mismo día (**`Testimonials.astro`, 10ma vuelta — fix real: "creo que no es gap lo que los separa, creo que regresamos a donde estaba". El `sm:basis-[calc(50%-1rem)]` de cada tarjeta restaba 16px asumiendo el gap VIEJO de 32px — al bajar el gap real a 10px (9na vuelta) sin actualizar esta cuenta, cada tarjeta quedó 6px más angosta de más por lado, dejando un espacio "fantasma" que no era el `gap` de flexbox. Fix: `sm:basis-[calc(50%-1rem)]` → `sm:basis-[calc(50%-5px)]` (mitad exacta del gap real de 10px). La fórmula de `lg` no se tocó, su gap no cambió** — ver PROGRESS.md.)
> Antes, mismo día (**`Testimonials.astro`, 9na vuelta — gap del carousel a 10px SOLO entre 540–1079px: "de 540px menor a 1080px que el gap entre elementos que sea 10px". `data-testimonials-track`: `gap-8` → `gap-8 sm:gap-2.5 lg:gap-8` (`gap-2.5` = 10px exacto). Se mantienen 32px en `base` (1 tarjeta) y `lg`+ (grid de escritorio). El grid estático (≤3 testimonios, sin carousel) no se tocó, fuera de alcance del pedido** — ver PROGRESS.md.)
> Antes, mismo día (**`Testimonials.astro`, 8va vuelta — el Tech Lead encontró la causa él mismo: "el container de la section se cambio a px-0, pero que menores a 540px se regrese el px-5... el wrapper del carousel seguía con `-mx-4 sm:-mx-6 lg:mx-0` sera mejor que regrese a mx-0". Contenedor padre: `px-0 sm:px-0 lg:px-8` → `px-5 sm:px-0 lg:px-8` (fullwidth real acotado a 540–1079px, por debajo de 540px vuelve el padding de 20px). Heading/grid estático/botón: su inset propio pasa de `px-5 ... lg:px-0` a `sm:px-5 ... lg:px-0` (ya no en `base`). Wrapper del carousel: `w-full` → `mx-0 w-full`. Efecto a vigilar sin corregir: el `ml-5`/`pr-5` del primer/último item real (6ta vuelta) podría duplicarse con el padding del padre por debajo de 540px (40px vs 20px de los items del medio) — pendiente de confirmar en vivo** — ver PROGRESS.md.)
> Antes, mismo día (**`Testimonials.astro`, 7ma vuelta — fix real: "en mobile se sigue viendo igual". El wrapper del carousel tenía `-mx-4 sm:-mx-6 lg:mx-0`, pensado para cancelar el padding que el contenedor padre de la sección tenía ANTES; la 3ra vuelta del mismo día cambió el padding del padre a `px-0 sm:px-0` por debajo de `lg` pero este margen negativo se quedó sin actualizar, cancelando un padding que ya no existía — resultado: margen negativo real, wrapper más ancho que el viewport. Fix: `w-full` a secas (sin margen negativo) — edge-to-edge por debajo de `lg` (padre ya sin padding), dentro del padding compartido en `lg`+ (sin breakout, como se quería)** — ver PROGRESS.md.)
> Antes, mismo día (**`Testimonials.astro`, 6ta vuelta: el gutter de borde del carousel baja de 24px a 20px (`ml-6`/`pr-6` → `ml-5`/`pr-5`) — pedido del Tech Lead con captura de DevTools: "tiene una separacion o espacio al margen de la ventana, si lo comparamos con el pr-5 o mr-5 del contenido de los elementos de las otras secciones". 20px es el gutter estándar del resto del sitio (mismo valor del carousel de `Features.astro`); el 24px de la vuelta anterior quedaba inconsistente** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro`: fix real, último item del carousel se veía cortado en mobile — el div de contenido inmediato dentro de la `<section>` nunca tuvo `w-full`. Cuando `.js-pin-active` (GSAP) pone `display: flex` en la sección para centrar verticalmente, ese div se vuelve un flex item con `flex-basis: auto`/`flex-grow: 0` y deja de llenar el 100% del ancho automáticamente — queda más angosto que el viewport, y el carousel de adentro se calcula contra ese ancho recortado. Confirmado con capturas de DevTools del Tech Lead (div medía 561.88px, franja de espacio libre visible a la derecha del overlay de flexbox). Fix: `w-full` explícito en ese div** — ver PROGRESS.md.)
> Antes, mismo día (**Escala H2 compartida (`RichText`/`Split`/`Testimonials`/`Logos`/`Cta`), 5ta vuelta — se quita el techo `5xl:text-5xl` (1700px+): "no necesitamos mas grande se ve mal". Clase final: `text-2xl sm:text-3xl 4md:text-4xl` = 24/30/36px, 36px como techo desde 1024px en adelante. Efecto secundario positivo: desaparece el único empate que quedaba con el H1 de página (antes ambos 48px en 1700px+) — el H1 sigue subiendo a 48px desde 1024px, el H2 queda fijo en 36px, jerarquía "H1 siempre más grande" restablecida en TODO el rango, sin empates** — ver PROGRESS.md.)
> Antes, mismo día (**Escala H2 compartida, 4ta vuelta — mismos 4 tamaños de la 3ra vuelta, pero el 2do corte pasa de 768px (`md`) a 1024px (`4md`, breakpoint custom del proyecto): "menores a 1024px que sea text-3xl". Clase final: `text-2xl sm:text-3xl 4md:text-4xl 5xl:text-5xl` = 24/30/36/48px. Efecto secundario positivo: de los 2 empates con el H1 de página que había en la 3ra vuelta, queda solo 1 (`5xl`/1700px+, ambos 48px) — en `md`/768–1023px el H2 vuelve a ser menor que el H1** — ver PROGRESS.md.)
> Antes, mismo día (**Escala H2 compartida, 3ra vuelta y spec FINAL — reemplazo completo por la spec numérica exacta del Tech Lead: "<540px `text-2xl`, <768px `text-3xl`, <1700px `text-4xl`, >1700px `text-5xl`". Clase final: `text-2xl sm:text-3xl md:text-4xl 5xl:text-5xl` = 24/30/36/48px (ya no hay escalones `3md`/`2xl` propios ni el valor custom 40px de antes — el tramo final es el `text-5xl` real de Tailwind, 48px). Efecto conocido sin corregir: el H1 de página empata con este H2 en 2 rangos (768–1023px y 1700px+, ambos en 36px y 48px respectivamente) — pendiente confirmar con el Tech Lead si también hace falta ajustar el H1** — ver PROGRESS.md.)
> Antes, mismo día (**Escala H2 compartida, 2da vuelta — se quita también `sm:text-4xl`: por debajo de `md`/768px el H2 queda uniforme en `text-3xl` (30px), sin el salto a 36px que había en `sm`/540–767px. Escala final: `text-3xl 3md:text-3xl 2xl:text-4xl 5xl:text-[40px]` = 30/30/36/40px. Esto también resuelve solo el efecto colateral de la vuelta anterior (H1/H2 empatando en 768–959px) — el H2 ahí vuelve a 30px, de nuevo menor que el H1 (36px)** — ver PROGRESS.md.)
> Antes, mismo día (**Escala H2 compartida, 1ra vuelta: se quita el escalón `md:text-2xl` (24px, 768–959px, un "valle" no intencional entre `sm` 36px y `3md` 30px)** — ver PROGRESS.md.)
> Antes, mismo día (**`Testimonials.astro`, 5ta vuelta — fix real: el `mr-6` del último item no se veía porque el wraparound (`cloneNode`) siempre agrega clones detrás de él, así que ese item nunca es el último hijo real del track y su margen quedaba a mitad de camino, no en el borde donde el usuario realmente termina de scrollear. Se quita `mr-6` del item, pasa a `pr-6` en el propio track — vive en el contenedor, aparece después de lo que sea que ocupe el último lugar (real o clon). El `ml-6` del primer item queda igual (borde estable, nada se clona antes)** — ver PROGRESS.md.)
> Antes, mismo día (**`Testimonials.astro`, 4ta vuelta sobre el mismo bug — fullwidth extendido de "solo el wrapper del carousel" a TODO el contenedor de la sección (heading + carousel + botón "Ver más") por debajo de `lg`/1080px, pedido explícito tras corrección del Tech Lead ("me refiero no al gap, me refiero al px-6"): "que sea fullwidth solo menor a 1080px en esta seccion y hay que agregar el ml-6 y mr-6 al primero y ultimo elemento para que pueda quedar estetico". Contenedor padre `px-4 sm:px-6 lg:px-8`→`px-0 sm:px-0 lg:px-8`; heading/grid estático/botón reciben su propio inset (`px-5`/`mx-5 ... lg:px-0`/`lg:mx-0`) para no pegarse al borde (el carousel SÍ busca ese efecto). `ml-6`/`mr-6` agregado al primer/último item real del carousel; clones del wraparound (`cloneNode(true)`) los stripean explícito para no heredar el hueco a mitad de track. Pendiente confirmar visualmente en navegador real** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro`, 4ta y última vuelta sobre el mismo bug — se abandona el enfoque manual (wheel/touch/pointer + `preventDefault()`) por completo y se migra a GSAP ScrollTrigger, pedido explícito del Tech Lead: "tal vez podria usarse HORIZONTAL CAROUSEL ON SCROLL - GSAP scrolltrigger?". `pin: true` + `scrub` sobre el track: el usuario scrollea la página 100% nativo (wheel o touch, sin `preventDefault()`), GSAP solo lee la posición de scroll y anima un `transform`, la sección queda fijada en pantalla mientras dura el recorrido. Resuelve de raíz los 3 fracasos anteriores (frágiles por pelear contra el scroll nativo) Y el bug del navbar tapando la sección (el `start` del trigger ya considera su altura). Scope confirmado: solo `<960px` vía `gsap.matchMedia()` ("el enfoque tactil solo en tablet y mobile pero no en desktop"), desactivado con `prefers-reduced-motion` (fallback: Pointer Events manual, sin scroll-jack). Nueva dependencia `gsap` (`^3.15.0`, ScrollTrigger gratis) agregada a `package.json` — NO instalada en este sandbox (sin acceso a la registry de npm, `403 Forbidden`), pendiente `npm install` en entorno real antes de poder buildear** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro`, 3ra vuelta sobre el mismo bug: el direction-locking de Touch Events (2da vuelta) tampoco resolvió el swipe manual, confirmado en vivo por el Tech Lead ("aun no funciona el swipe manual y de scroll horizontal para continuar scrolleando abajo y viceversa") — se abandona `touch-action`/Touch Events por completo y se reemplaza el arrastre manual por Pointer Events (`scrollLeft` asignado directo, patrón YA PROBADO de `Testimonials.astro`), con snap-to-nearest-card al soltar. Trade-off explícito: el scroll-jack FORZADO (vertical→horizontal automático) queda solo para `wheel`/trackpad — en touch, el scroll vertical vuelve a pasar nativo y el usuario navega arrastrando con el dedo** — ver PROGRESS.md. Este intento también resultó insuficiente, ver entrada de arriba: reemplazado por completo con GSAP.)
> Antes, mismo día (**`Features.astro`: fix real de 2 bugs reportados en vivo por el Tech Lead — (1) carousel "pegado", swipe manual roto: `onTouchMove` interceptaba también los swipes horizontales por el temblor vertical incidental de cualquier gesto manual, bloqueando el pan-x nativo vía `preventDefault()`; fix con direction-locking (compara `|ΔX|` vs `|ΔY|` acumulados, decide UNA vez por gesto). (2) Navbar sticky tapando la sección al arrancar el hijack: se agrega `dockBelowHeaderIfNeeded()` — mide `[data-site-header]` en vivo y corrige el scroll de página ANTES de mover el track, para que el hijack nunca arranque con la sección tapada** — ver PROGRESS.md. Este intento resultó insuficiente, ver entrada de arriba.)
> Antes, mismo día (**`Features.astro`: peek del carousel más grande (`basis-[85%]`→`basis-[75%]`) + gutter fijo de 20px (`px-5`) alineando heading/carousel + último ítem con `snap-end` para terminar exacto en ese margen** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro`: scroll-jack en el carousel (scroll vertical → horizontal mientras no se llegó al extremo, `wheel`+`touchmove` en el `<section>`, respeta `prefers-reduced-motion`) + contenedor fullwidth por debajo de 960px (`content_width` de Filament solo aplica desde `3md`)** — ver PROGRESS.md. Pendiente probar en dispositivo real, riesgo de fricción en swipes rápidos.)
> Antes, mismo día (**`Features.astro`: responsive real — grid 3 columnas desde 960px (`3md`), carousel de 1 tarjeta con "peek" (~15% de la siguiente asomando, principio de continuidad de Gestalt) por debajo, sin auto-avance/dots/wraparound (a diferencia de `Testimonials.astro`/`Logos.astro`), 0 JS nuevo, scroll-snap nativo** — ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro`, fix real: heading (pretitle/título/subtítulo) sin gap estandarizado antes del grid** — usaba `margin-bottom` por elemento (se notaba con SOLO título, sin subtítulo: quedaba en 8px). Reestructurado al mismo patrón de `Testimonials.astro`/`RichText.astro`/`Logos.astro`: cluster condicionado como un ítem de `flex flex-col gap-12`, separación de 48px garantizada sin importar qué campos estén presentes. Auditados también `Cta.astro`/`Logos.astro` (ya correctos) y `ServicesGrid.astro`/`Faq.astro`/`LegalNotice.astro` (stubs sin pretitle/subtitle, fuera de alcance). Ver PROGRESS.md.)
> Antes, mismo día (**"Sobre CICA": contenido — se agregan bloques `testimonials`/`logos` a la página (solo seeder en genesis, sin cambios de código acá; `Testimonials.astro`/`Logos.astro` ya soportan cualquier página que los use)** — ver PROGRESS.md en genesis. También fix real de `Features.astro`: `item.description` se veía con peso de subtítulo (14px/`font-light`), corregido a cuerpo de texto (16px/`font-normal`). Ver PROGRESS.md.)
> Antes, mismo día (**`Features.astro`: `item.content_format` (por item) → `properties.list_style` (por bloque)** — corrección del Tech Lead sobre el nombre y ubicación del selector "ninguno/lista/grid". Ver PROGRESS.md y ADR-048 en genesis.)
> Antes, mismo día (**`Features.astro` reescrito por completo** — fondo sólido/degradado/imagen, `feature_style`/`card_rounded`, imagen o avatar-inicial por item, `content_format` (párrafo/lista/grid). Fix real: leía `item.subtitle`, Filament siempre guardó `item.description` — la descripción nunca se veía. Ver PROGRESS.md y ADR-047 en genesis.)
> Antes, mismo día (**`RichText.astro`, fix real: cluster pretitle/título/subtítulo se renderizaba vacío, empujaba el texto hacia abajo** — el `<div>` cluster no estaba condicionado a que hubiera alguno de los 3 campos, así que en un `rich_text` sin heading el `gap-6` del padre se aplicaba de más "fantasma" antes del cuerpo. Ahora el `<div>` entero está condicionado (`{(pretitle||title||subtitle) && (...)}`). Ver PROGRESS.md.)
> Antes, mismo día (**`Hero.astro`, 6ta vuelta: padding X del CTA vuelve a `px-5`** — revierte un escalón el `px-3` de la 4ta vuelta, sin volver al `px-9` original. Ver PROGRESS.md.)
> Antes, mismo día (**`Hero.astro`, 5ta vuelta: altura del CTA fijada a 48px en mobile** — `h-[60px]`→`h-12`; desde `sm` sigue en `sm:h-14`. Ver PROGRESS.md.)
> Antes, mismo día (**`Hero.astro`, 3ra y 4ta vuelta: CTA seguía en 2 líneas en mobile — se ataca el ancho disponible** — texto `text-xs leading-[18px]` (12px), padding `px-9`→`px-3`, `gap-2`→`gap-1.5`, ícono a clase responsive `size-4 sm:size-6`, `whitespace-nowrap` agregado. Altura sin tocar. Ver PROGRESS.md. Pendiente confirmar en mobile real que ya entra en una línea.)
> Antes, mismo día (**`Hero.astro`, 2da vuelta: CTA en mobile quedó muy chico, sube un peldaño intermedio** — `text-sm`→`text-[15px] leading-[26px]`, botón `h-14 px-8`→`h-[60px] px-9` en mobile; desde `sm` (640px) vuelve a los valores originales. Ver PROGRESS.md.)
> Antes, mismo día (**`Hero.astro`: leading del título un poco más alto (`leading-[0.95]`→`leading-[1.05]`) + label del CTA reducido en mobile (`text-base`→`text-sm leading-6` base, `sm:text-base sm:leading-7` desde 640px)** — botón sin cambio de tamaño, solo el texto adentro. Ver PROGRESS.md.)
> Antes, mismo día (**`Heading.astro`, 15ta vuelta: `leading-tight` en el título + `text-base leading-snug sm:text-lg sm:leading-normal` en el subtítulo** — mejora el wrap a 2 líneas en mobile/375px de los títulos nuevos con voseo. Ver PROGRESS.md.)
> Antes, mismo día (**`Heading.astro`, 14ta vuelta: overlay `h-[70%]` de la sección, no `h-64` fijo ni `inset-0`** — pedido explícito: "solo el 70%". Ver PROGRESS.md.)
> Antes, mismo día (**`Heading.astro`, 13va vuelta: overlay `h-64` calculado contra el alto real del navbar (88px en reposo), no `inset-0`** — el tramo sólido del degradado debe quedar detrás del navbar, no visible. Ver PROGRESS.md.)
> Antes, mismo día (**`Heading.astro`, 12va vuelta: overlay `inset-0` (proporcional al alto real de la sección) en vez de banda fija de 176px** — los 176px fijos cubrían más de la mitad del banner en vivo. Ver PROGRESS.md.)
> Antes, mismo día (**`Heading.astro`, 11va vuelta: overlay de velo plano → degradado (`linear-gradient` + `color-mix()`), spec exacto de Figma** — color sólido 0-35% → transparente 100%. Ver PROGRESS.md.)
> Antes, mismo día (**`Heading.astro`, 10ma vuelta: escala del H1 corregida en `base`/`sm` — quedaba más chica que el H2 de las secciones** — `text-[2rem] sm:text-[2.5rem] md:text-4xl lg:text-5xl` (32/40/36/48px), siempre por encima del H2 compartido. Ver PROGRESS.md.)
> Antes, mismo día (**`Heading.astro`, 9na vuelta: `pt-8`→`pt-4` + `gap-2` eliminado del wrapper de contenido** — ver PROGRESS.md.)
> Antes, mismo día (**`Heading.astro`, 8va vuelta: padding vertical del contenido asimétrico `pt-8 pb-0`, para compensar el navbar overlay** — texto corrido hacia abajo, ver PROGRESS.md.)
> Antes, mismo día (**`Heading.astro`, 7ma vuelta: `md`/`lg` de altura revertidos a `40vh`/`34vh`, solo mobile/`sm` quedan reducidos** — `min-h-[34vh] sm:min-h-[30vh] md:min-h-[40vh] lg:min-h-[34vh]`. Ver PROGRESS.md.)
> Antes, mismo día (**`Heading.astro`, 6ta vuelta: altura reducida otra vez en mobile/tablet** — `min-h-[34vh] sm:min-h-[30vh] md:min-h-[28vh] lg:min-h-[26vh]`. Ver PROGRESS.md.)
> Antes, mismo día (**`Heading.astro`, 5ta vuelta: título `font-black` (900) en vez de `font-bold`** — sin `italic`, ver PROGRESS.md.)
> Antes, mismo día (**`Heading.astro`, 4ta vuelta: título un escalón más chico en toda la escala** — `text-2xl sm:text-3xl md:text-4xl lg:text-5xl` (24→30→36→48px), 100% clases estándar de Tailwind. Ver PROGRESS.md.)
> Antes, mismo día (**`Heading.astro`, 3ra vuelta: paso `md` del título de 48px a 40px vía `text-[2.5rem]`** — escala `text-3xl sm:text-4xl md:text-[2.5rem] lg:text-6xl`. Ver PROGRESS.md.)
> Antes, mismo día (**`Heading.astro`, 2da vuelta: título con escala propia `text-3xl sm:text-4xl md:text-5xl lg:text-6xl` (30→36→48→60px), distinta de `Hero.astro`** — ver PROGRESS.md.)
> Antes, mismo día (**`Heading.astro`: fondo `bg-top` (cover + position top center) + altura en `vh` decreciente por breakpoint** — ver PROGRESS.md.)
> Antes, 2026-09-05 (**`Heading.astro` reescrito: stub → implementación real** — banner "Sobre CICA" con imagen de fondo responsiva, overlay configurable, decoradores y filtros. Ver PROGRESS.md. Cruza con fix real en genesis, mismo día, del mismatch `'waves'`/`'wave'` en el Select del bloque `heading`.)
> Antes, 2026-09-02 (**Finalizada la integración con la página HOME y sus bloques**) — `npm run check` verificado en verde sobre 34 archivos (0 errores, 0 warnings, 0 hints).

---

## Resumen ejecutivo

| Campo | Valor |
|-------|-------|
| Proyecto | Sitio público de CICA360 (Cliente 0 de Stamless) |
| Fase | Integración visual y funcional de la Home finalizada. `npm run check` en verde (34 archivos, 0 errores) |
| Framework decidido | Astro (SSG, islands architecture) — ver ADR-001 |
| Backend consumido | Stamless API REST v1 (`api.stamless.io/v1/cica360/...`) |
| Hosting objetivo | Shared hosting del cliente, sin Node/npm |
| Salud general | 🟢 `npm run check` verificado en verde (0 errores, 0 warnings en 34 archivos). Certificado TLS local configurado vía `NODE_EXTRA_CA_CERTS` con mkcert. |

---

## Qué está hecho

- [x] Decisión de stack frontend: **Astro SSG + islands**, React/Preact solo en bloques interactivos (`contact_form`, eventualmente el slider del hero) — ver ADR-001 en `DECISIONS.md`.
- [x] Descartadas explícitamente: SPA de React puro (peor performance/SEO sin necesidad real de interactividad total) y Next.js con `output: 'export'` (pelea contra el propio framework: sin Image Optimization en runtime, sin API routes para ocultar el Bearer, pensado para correr con Node detrás).
- [x] Estrategia de formulario de contacto decidida: **proxy PHP server-side por defecto** (oculta el Bearer, casi todo shared hosting trae PHP+cURL aunque no traiga Node), con **token acotado a `forms:submit`** como fallback documentado si el hosting no soporta PHP — ver ADR-002.
- [x] Estructura de contexto creada (`docs/context/`) separada del repo del backend Stamless, para no mezclar decisiones/ADRs de un proyecto con el otro.
- [x] Manual del API Stamless (`docs/context/api/stamless-api-v1.md`) copiado/adaptado como referencia autosuficiente de este proyecto — no hace falta ir al repo del backend para saber el contrato de datos.
- [x] División de roles acordada (brief original de Grok, confirmado): Claude = capa de datos/infra (`api.ts`, tipos, rutas, proxy, build/deploy); Antigravity/Gemini = capa visual (layouts, componentes de bloque, fidelidad a CICA360).

## Qué está hecho (continuación — scaffold real, sesión 2)

- [x] **Fase 0 — Scaffold**: `package.json` (Astro ^7.2, `@astrojs/react` ^6, `@astrojs/sitemap`, Tailwind v4 vía `@tailwindcss/vite`, React 19, Prettier + plugins), `astro.config.mjs` (`output: 'static'`, sin SSR, integraciones React/sitemap/Tailwind), `tsconfig.json` (`astro/tsconfigs/strict`), `.env.example`, `src/env.d.ts`, `.gitignore`, `.editorconfig`, `.prettierrc.json`.
- [x] **Fase 1 — Capa de datos** (`src/lib/`): `types.ts` (todo el contrato del manual del API tipado: `Page`, `Post`, `Menu`, `Slider`, `Block`, `ContentLink`, `Media`, envelope, paginación), `errors.ts` (`ApiError` con `status`/`code`/`fields`, `ApiNetworkError`), `api.ts` (cliente fetch con timeout + reintentos acotados para errores transitorios, `getPages`/`getPage`/`getAllPages`/`getPosts`/`getPost`/`getAllPosts`/`getMenu`/`getSlider`/`getMedia`, guard `assertServerContext()` que tira si alguien intenta importarlo desde el navegador, y `submitContactForm()` — client-safe, nunca usa el token de `content:read`).
- [x] **Rutas** (`src/pages/`): `index.astro` (home resuelta por `is_home=true`, no por slug fijo), `[slug].astro` (`getStaticPaths` desde `GET /pages`, excluye la home), `blog/index.astro`, `blog/[slug].astro`, `404.astro`.
- [x] **Layout + bloques stub** (`src/layouts/`, `src/components/`): `BaseLayout.astro` (SEO básico, sin `<ClientRouter />` a propósito — ver decisión en `ARCHITECTURE.md` §7; ahora también carga Playfair Display + Inter), `Header.astro`/`Footer.astro` (reciben el menú ya resuelto, sin fetch propio), y un componente por cada uno de los 13 `block.type` conocidos. **9 siguen siendo stubs** sin diseño visual final (`ImageBlock`, `Cta`, `Features`, `Faq`, `ContactFormBlock`, `LegalNotice`, `Heading`, `Logos`, `ServicesGrid`), pendientes para Antigravity. **`Hero.astro`, `RichText.astro`, `Split.astro` y `Testimonials.astro` ya NO son stubs**: `Hero.astro` implementado como Hero Slider real (auto-avance, fade, wave decorator); `RichText.astro` (2026-08-31) implementado con personalización visual completa (fondo/color/alineación/ancho de contenido/padding/decoradores arriba+abajo con opacidad/flecha de scroll/enlace único opcional); `Split.astro` (2026-08-31) implementado como grid de 2 columnas con `content.media_position` alternando el lado de la imagen; `Testimonials.astro` (2026-08-31, con una segunda pasada la misma tarde corrigiendo contra el mockup real: avatares más grandes, tarjeta con fondo propio, cita sin comillas, firma en una línea, y carousel táctil cuando hay más de 3 testimonios) implementado como sección de fondo sólido con grid de tarjetas (avatar circular con fallback de iniciales, frase/firma en itálica) + botón "ver más" opcional — los 4 bloques por Claude a pedido directo del Tech Lead, cruzando puntualmente el límite de roles — ver `PROGRESS.md` para el detalle de cada uno. `BlockRenderer.astro` ignora tipos desconocidos sin romper la página (con warning solo en dev).
- [x] **`Logos.astro` — carousel de marcas/socios, antes stub vacío** (2026-08-31): grilla estática de hasta 7 logos (`grid-cols-3 sm:grid-cols-4 md:grid-cols-5 2lg:grid-cols-7`); con más de 7, carousel paginado de a 7 (cada "página" del carousel es una grilla completa, no un logo individual — a diferencia de `Testimonials.astro`) con flechas, dots dorados y drag por Pointer Events (mouse+touch+pen), mismo patrón ya establecido en el resto del sitio. Filtro grayscale/opacidad por logo (`properties.media_filter_grayscale`/`media_opacity`, ya genéricas — reusadas de `split`, no nuevas) que se quita en `:hover`/`:focus-visible` revelando el logo a color real. `content.items[]` sin cambios de forma (`media`/`url`), `Media` ya existía en `types.ts`, `BlockRenderer.astro` ya ruteaba `logos` — cero cambios de tipos/routing, todo el trabajo fue dentro del componente.
- [x] **`Header.astro` — menú mobile fullscreen** (2026-08-31, cruzando puntualmente el límite Claude=datos/Antigravity=visual, mismo criterio ya usado en `Hero.astro`): por debajo de `3md` (960px) la topbar muestra hamburguesa/logo centrado/CTA-ícono en vez del menú desktop completo; al tocar la hamburguesa se abre un overlay fullscreen (`cicaindigo-500`) con la nav vertical centrada (mismo hover/active que desktop), soporte de submenús (`item.children`, colapsables) y el CTA completo abajo. Detalle completo en `PROGRESS.md`.
- [x] **Formulario de contacto**: `src/components/islands/ContactForm.tsx` (única isla React real del proyecto, `client:visible`, con honeypot, validación básica, estados idle/submitting/success/error, errores por campo desde `errors.fields`). `public/contacto.php` — proxy PHP compatible desde PHP 7.4+ (rate limit best-effort por IP, honeypot server-side extra, forward por cURL a Stamless, nunca expone el token) + `public/contacto.config.example.php` (plantilla, la real — `contacto.config.php` — va en `.gitignore` y se crea a mano en el servidor).
- [x] **Infraestructura/seguridad adicional**: `public/.htaccess` (headers de seguridad + CSP inicial + cache agresivo de assets con hash + forzado de HTTPS — CSP no probada en hosting real todavía), `public/robots.txt` + `@astrojs/sitemap`, `.github/workflows/build-and-deploy.yml` (CI de build con `astro check`+`astro build`; el paso de deploy por FTP queda comentado hasta confirmar credenciales reales del hosting).

### ⚠️ Limitación importante de esta sesión — leer antes de confiar ciegamente en el scaffold

Este scaffold se generó en un entorno **sin acceso a la registry de npm** (mismo tipo de restricción de red que ya afectaba a Composer/PHP en el repo del backend Stamless). Concretamente:

- **No se pudo correr `npm install`** — no hay `node_modules/` ni `package-lock.json` generados todavía.
- **No se pudo correr `astro check` ni `astro build` reales** — toda la verificación fue manual: balance de paréntesis/llaves/corchetes por script, validación de JSON/YAML, y una revisión cuidadosa línea por línea de cada archivo (incluyendo corregir en el camino un uso incorrecto de `<ViewTransitions />`, que en la versión actual de Astro es `<ClientRouter />` — se optó por sacarlo del todo a favor de CSS nativo, ver `ARCHITECTURE.md` §7).
- **Ya se corrió tres veces, en real**: la primera corrida encontró 4 errores de TypeScript (índices de firma faltantes) — corregidos. La segunda expuso un warning de deprecación de `FormEvent` (fix anterior insuficiente, corregido a `SubmitEvent`) y un error bloqueante de certificado TLS local. La tercera **quedó confirmada en verde**: `astro check` → 0 errores/0 warnings/0 hints, `astro build` → 10 páginas generadas. Único punto suelto: el bypass de TLS automático generaba igual un warning de Node aunque ya no hiciera falta — corregido (ver `PROGRESS.md`, entrada "Tercer `npm run build` real"): ahora es opt-in explícito (`STAMLESS_DEV_INSECURE_TLS`), y se documentó la solución real (certificado local confiable vía mkcert) en el README. **Falta que el humano instale mkcert y confirme una corrida sin ese warning en absoluto** — paso manual fuera del repo.

## Qué falta (alto nivel, ver detalle en `TASK.md`)

- [ ] **Ejecutar `npm install` + `npm run build` reales** y corregir lo que el sandbox no pudo verificar (ver limitación arriba) — bloqueante antes de seguir sumando features.
- [ ] **Fase 2** (Antigravity) — reemplazar los stubs de `src/components/blocks/*` y `src/layouts/BaseLayout.astro` con el diseño visual real, fidelidad a las capturas de CICA360.
- [ ] **Fase 3, cierre** — generar el token real de `forms:submit`, completar `contacto.config.php` en el servidor (no en git), probar el submit end-to-end.
- [ ] **Fase 4, cierre** — confirmar credenciales FTP/SFTP del hosting real y descomentar el paso de deploy en `.github/workflows/build-and-deploy.yml`; configurar Cloudflare delante del dominio.
- [ ] **Fase 5** — QA de performance (Lighthouse/PageSpeed) y auditoría real del JS que se manda al navegador, una vez que haya un build real corriendo.
- [ ] **Fase 6** (post-MVP, opcional) — Rebuild automático al publicar contenido en Filament (webhook `repository_dispatch`).

---

## Bloqueadores actuales

| Bloqueador | Impacto | Estado |
|------------|---------|--------|
| .env del Backend (`/Users/edu/Storage/webapps/projects/genesis/.env`) con dominios desactualizados | `api.stamless.host` responde con 404 porque el backend aún espera `api.genesisly.host`. Esto bloquea el build del frontend. | Pendiente de que el humano actualice el `.env` del backend y reinicie el servidor de desarrollo |
| Token real del API Stamless (Console → Desarrolladores → API Tokens) | No se puede empezar Fase 1 sin un token de `content:read` para desarrollo | Pendiente de generar en Console (`console.stamless.host`) |
| Confirmación del tipo de shared hosting (¿tiene PHP? ¿permite subir por FTP/SFTP? ¿cron?) | Define si el formulario usa proxy PHP o token acotado, y cómo se automatiza el deploy | Pendiente de confirmar con el cliente/hosting |
| Media real de CICA360 (R2 todavía no integrado en el backend, ver backend `CURRENT_STATE.md`) | Los bloques de imagen van a venir `null` hasta que el backend tenga media cargada | No bloquea el desarrollo del front (se puede maquetar con placeholders), sí bloquea el contenido final |

---

## Notas para el próximo agente

- Leer primero: `AGENTS.md`, este archivo, `TASK.md`, `ARCHITECTURE.md`, `DECISIONS.md`, y el manual del API en `docs/context/api/stamless-api-v1.md`.
- Este repo es independiente del backend Stamless — no reabrir decisiones de arquitectura del backend acá (dominios, excepciones, multi-tenancy del backend, etc. viven en el otro repo).
- No hay código todavía: el siguiente trabajo es el scaffold real (Fase 0 en `TASK.md`).
- Antes de handoff: actualizar este archivo + `TASK.md` + entrada en `PROGRESS.md`.
